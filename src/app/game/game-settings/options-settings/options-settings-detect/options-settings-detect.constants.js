export const DEFAULT_SETTINGS = {
  revealing: true,
  unlimitedFlags: true,
  marks: true,
  misplacedFlagHint: false
};

export const MODE_PROPERTIES = ['revealing', 'unlimitedFlags', 'marks', 'misplacedFlagHint'];