export const DEFAULT_SETTINGS = {
  flagging: true,
  unlimitedFlags: true,
  marks: true,
  misplacedFlagHint: false,
  openStrategy: true
};

export const DEFAULT_SETTINGS_NO_FLAGGING = {
  flagging: false,
  unlimitedFlags: false,
  marks: false,
  misplacedFlagHint: false,
  openStrategy: false
};

export const MODE_PROPERTIES = ['unlimitedFlags', 'marks', 'misplacedFlagHint', 'openStrategy'];
