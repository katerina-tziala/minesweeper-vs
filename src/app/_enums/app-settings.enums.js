"use strict";

const Theme = {
  Default: "light",
  Dark: "dark",
};
Object.freeze(Theme);

const MineType = {
  Bomb: "bomb",
  Bahai: "bahai",
  Virus: "virus",
};
Object.freeze(MineType);

export { Theme, MineType };
