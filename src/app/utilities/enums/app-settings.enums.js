'use strict';

const THEME = {
    Default: 'default',
    Dark: 'dark',
}
Object.freeze(THEME);

const MINETYPE = {
    Bomb: 'bomb',
    Bahai: 'bahai',
    Virus: 'virus',
}
Object.freeze(MINETYPE);

export { THEME, MINETYPE};